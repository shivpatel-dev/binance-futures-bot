# ============================================================
#  Binance Futures Bot - Environment Configuration (v7 Stable)
# ============================================================
#  ⚙️  Purpose:
#  This file provides a safe, shareable template of required
#  environment variables. Copy it to a new file named `.env`
#  and fill in your actual keys and values before running.
# ============================================================

# === Binance API Credentials ===
# Your personal API key and secret from Binance.
# These should always remain private.
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# === Trading Settings ===
# Symbol pair to trade (e.g., BTCUSDT, ETHUSDT)
SYMBOL=BTCUSDT

# Leverage level for futures trading
LEVERAGE=20

# Position mode: "ISOLATED" or "CROSSED"
POSITION_MODE=ISOLATED

# Trade size per order (in USDT or equivalent)
TRADE_SIZE=100

# === Take Profit / Stop Loss Configuration ===
# Example: 1 = 1% TP and SL
TAKE_PROFIT_PERCENT=1
STOP_LOSS_PERCENT=1

# === Telegram Integration ===
# If your bot reads trade calls from Telegram,
# provide your Telegram Bot Token and Channel ID here.
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHANNEL_ID=your_channel_id_here

# === Mode Settings ===
# Set to "DRYRUN" for testing (no real trades)
# or "LIVE" for actual execution.
MODE=DRYRUN

# === Logging & Output ===
# Log file name where activity will be saved.
LOG_FILE=bot.log

# Set to TRUE to enable console debug logs.
DEBUG_MODE=TRUE

# === System Notes ===
# Ensure this file is renamed to `.env` before running locally.
# Keep `.env` excluded from commits using .gitignore.
# ============================================================
